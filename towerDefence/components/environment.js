import GameComponent from "./game-component.js";

//let treeSize = 70;

//let defaults = { x: 0, y: 0, size: treeSize };

export default class Environment extends GameComponent {
  //{ x = 0, y = 0, size = treeSize, image, age = 12 }
  //constructor(props) {
  //let this={};
  // this.x = x;
  // this.y = y;
  // this.size = size;
  // this.image = image;
  // this.age = age;
  // Object.assign(this, defaults, props);
  //return this;
  //}
}

// function createTree({ x, y }) {
//   return {
//     image: IMAGES.OAK,
//     size: treeSize,
//     x: x,
//     y: y
//   };
// }
